@model DirectoryManager.Web.Models.SponsoredListingOffersViewModel
@using DirectoryManager.Data.Enums
@using DirectoryManager.Web.Constants
@using DirectoryManager.Utilities.Helpers

@{
    ViewData[StringConstants.TitleHeader] = "Sponsored Listing Offers";
}

<h1>@ViewData[StringConstants.TitleHeader]</h1>

<hr />
@await Html.PartialAsync("_BackToHome")
<hr />

<h2>Offers</h2>

<p>These are the rates for advertising. The default prices are used unless there is a specific price for a subcategory, in which case that is the rate.</p>

<br />
<p>
    <a href="~/sponsoredlisting">Buy Advertising</a>
</p>

<hr />

<h3>@EnumHelper.GetDescription(SponsorshipType.MainSponsor)</h3>

<table>
    <thead>
        <tr>
            <th>Category > Subcategory</th>
            <th>Days</th>
            <th>Price (@DirectoryManager.Data.Enums.Currency.USD.ToString())</th>
        </tr>
    </thead>
    <tbody>
        @foreach (var offer in Model.MainSponsorshipOffers)
        {
            <tr>
                <td>@offer.CategorySubcategory</td>
                <td>@offer.Days</td>
                <td>@offer.Price.ToString(DirectoryManager.Common.Constants.StringConstants.CurrentFormat)</td>
            </tr>
        }
    </tbody>
</table>

<hr />

<h3>@EnumHelper.GetDescription(SponsorshipType.SubcategorySponsor)</h3>
<table>
    <thead>
        <tr>
            <th>Category > Subcategory</th>
            <th>Days</th>
            <th>Price (@DirectoryManager.Data.Enums.Currency.USD.ToString())</th>
        </tr>
    </thead>
    <tbody>
        @foreach (var offer in Model.SubCategorySponsorshipOffers)
        {
            <tr>
                <td>@offer.CategorySubcategory</td>
                <td>@offer.Days</td>
                <td>@offer.Price.ToString(DirectoryManager.Common.Constants.StringConstants.CurrentFormat)</td>
            </tr>
        }
    </tbody>
</table>

