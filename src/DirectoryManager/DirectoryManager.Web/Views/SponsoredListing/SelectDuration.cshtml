@using DirectoryManager.Web.Constants;
@using DirectoryManager.Web.Models;
@using System.Globalization;
@using DirectoryManager.Data.Enums
@model IEnumerable<SponsoredListingOffer>
@inject DirectoryManager.Web.Services.Interfaces.ICacheService _cacheHelper

@{
    ViewData["Title"] = "Select Listing Duration";
    var offersList = Model.ToList();
    int defaultChoiceId = 1;
}

<h1>@ViewData["Title"]</h1>

<a href="~/sponsoredlisting/selectlisting">&lt; Back</a>

<hr />

<p><i>@Html.Raw(_cacheHelper.GetSnippet(SiteConfigSetting.PaymentCurrencyMessage))</i></p>

<form method="post">
    <div class="form-group">
        <label>Options:</label>
        @for (var i = 0; i < offersList.Count; i++)
        {
            var offer = offersList[i];
            <div class="form-check">
                <label class="form-check-label" style="display: flex; align-items: center;">
                    <input class="form-check-input" type="radio" name="SelectedOfferId" value="@offer.Id" @(offer.Id == defaultChoiceId ? "checked" : string.Empty)>
                    @offer.Description - @offer.USDPrice.ToString("C", new CultureInfo(StringConstants.DefaultCulure)) USD
                </label>
            </div>
        }
    </div>
    <button type="submit" class="btn btn-primary">Select</button>
</form>
